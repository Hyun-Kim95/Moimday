# SQLite (로컬만): provider를 schema.prisma에서 sqlite로 바꾼 뒤 file:./dev.db
DATABASE_URL="postgresql://moimday:moimday@localhost:5432/moimday"
JWT_SECRET="change-me-in-production"
JWT_ACCESS_TTL_SEC=3600
JWT_REFRESH_TTL_SEC=2592000
PORT=3000

# 소셜 OAuth (서버 토큰 검증)
KAKAO_REST_API_KEY=
GOOGLE_OAUTH_CLIENT_IDS=
APPLE_CLIENT_IDS=

# FCM (선택)
FCM_ENABLED=false
