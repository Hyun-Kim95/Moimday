services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: Moimday
      POSTGRES_PASSWORD: Moimday
      POSTGRES_DB: Moimday
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
